# Конфигурационный файл окружения для Telegram чат-бота
# Хранит чувствительные данные и настройки
# Убедитесь, что файл имеет права доступа 600: chmod 600 .env
# Не добавляйте .env в Git (уже исключён через .gitignore)

# Токен Telegram-бота, полученный от @BotFather
# Замените 'your_bot_token' на реальный токен
TELEGRAM_BOT_TOKEN=your_bot_token

# Ключ шифрования для логов (генерируется с помощью cryptography)
# Выполните: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Замените 'your_encryption_key_here' на сгенерированный ключ
ENCRYPTION_KEY=your_encryption_key_here

# Путь к файлу базы данных SQLite
# По умолчанию: chatbot.db в корне проекта
DB_FILE=chatbot.db

# Порог голосов для мута пользователя
# По умолчанию: 5 голосов
MUTE_VOTE_THRESHOLD=5

# Временное окно для голосования за мут (в минутах)
# По умолчанию: 60 минут
MUTE_VOTE_WINDOW=60

# Длительность мута (в минутах)
# По умолчанию: 30 минут
MUTE_DURATION=30

# Текст правил чата (многострочный, используйте \n для переносов)
# По умолчанию: стандартные правила
RULES_TEXT=📜 Правила чата:\n\n1. Будьте вежливы.\n2. Без спама.\n3. Соблюдайте законы.

# Информация о боте (многострочный, используйте \n для переносов)
# По умолчанию: базовая информация
ABOUT_TEXT=🤖 Бот для управления чатом.\n\nВерсия: 1.0\nАвтор: xAI